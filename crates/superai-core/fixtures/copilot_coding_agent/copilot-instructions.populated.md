# Copilot Coding Agent Instructions

Apply the AGENTS.md guidelines. Use `AGENTS.md` at repo root plus this file.
Maintain Zero Data Retention contract where applicable.

## Setup

Install dependencies via `npm ci`.

## Validation

Run `npm test` and `npm run lint`.
